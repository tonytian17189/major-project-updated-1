@model major_project.Models.attires

<h4>attires</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="BorrowGears">
            <div>Confirm you want to borrow.</div> @Html.DisplayFor(model => model.attire)
            <div class="form-group">
                <input type="submit" value="Confirm" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>
<div>
    <a asp-action="Index">Back to List</a>
</div>
